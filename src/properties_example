# Required metadata
sonar.projectKey=ASF:lucene-solr
sonar.projectName=lucene-solr
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=./lucene-solr/trunk/.

# Language (comment for multi language - web)
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8

svnRepo=https://github.com/apache/lucene-solr
